<?php

namespace App\Notifications;

use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Notifications\Messages\MailMessage;
use Illuminate\Notifications\Notification;
use Illuminate\Support\HtmlString;

class WelcomeNotification extends Notification
{
    use Queueable;

    /**
     * Create a new notification instance.
     *
     * @return void
     */
    public function __construct()
    {
        //
    }

    /**
     * Get the notification's delivery channels.
     *
     * @param  mixed  $notifiable
     * @return array
     */
    public function via($notifiable)
    {
        return ['mail'];
    }

    /**
     * Get the mail representation of the notification.
     *
     * @param  mixed  $notifiable
     * @return \Illuminate\Notifications\Messages\MailMessage
     */
    public function toMail($notifiable)
    {
        $app_name = config('app.name');
        return (new MailMessage)
                    ->greeting("Hello {$notifiable->first_name},")
                    ->line(new HtmlString("Welcome to <strong>{$app_name}</strong>. We’re thrilled to see you here!"))
                    ->line("We’re confident that our service will help bring you fast, affordable, and quality healthcare.")
                    ->line("Over the next few weeks, we’ll be sending you a few more emails to help you gain maximum value from our service. We’ll share our favorite tips and provide some exciting behind-the-scenes information.")
                    ->line('Thank you for using our application!');
    }

    /**
     * Get the array representation of the notification.
     *
     * @param  mixed  $notifiable
     * @return array
     */
    public function toArray($notifiable)
    {
        return [
            //
        ];
    }
}
